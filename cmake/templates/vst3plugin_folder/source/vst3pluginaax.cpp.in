// @SMTG_SOURCE_COPYRIGHT_HEADER@

#include "public.sdk/source/vst/aaxwrapper/aaxwrapper_description.h"

#include "@SMTG_PREFIX_FOR_FILENAMES@cids.h"
#include "pluginterfaces/base/futils.h"

#define MANUFACTURER_ID CCONST('S', 'M', 'T', 'G') // @SMTG_MANUFACTURER_ID@
#define PRODUCT_ID 		CCONST ('A', 'G', 'S', 'B')// @SMTG_PRODUCT_ID@

#define NATIVE_3ID		'A', 'G', 'N' // @SMTG_NATIVE_3ID@
#define AUDIOSUITE_3ID 	'A', 'G', 'A' // @SMTG_AUDIOSUITE_3ID@

//------------------------------------------------------------------------
#if 0 // for additional outputs
AAX_Aux_Desc effAux_stereo[] = 
{
	// name, channel count
	{ "@SMTG_PLUGIN_NAME@ AUX2",  2 }, 
	{ nullptr }
};
#endif

//------------------------------------------------------------------------
#if 1 // MIDI inputs for instruments or Fx with MIDI input
AAX_MIDI_Desc effMIDI[] = 
{
	// port name, channel mask
	{ "@SMTG_PLUGIN_NAME@", 0xffff }, 
	{ nullptr }
};
#endif

//------------------------------------------------------------------------
// Input/Output meters
AAX_Meter_Desc effMeters[] = 
{
	// not used { "Input", CCONST ('A', 'G', 'I', 'n'),  0 /*AAX_eMeterOrientation_Default*/, 0 /*AAX_eMeterType_Input*/ },
	//{ "Output", kVuPPMId, 0 /*AAX_eMeterOrientation_Default*/, 1 /*AAX_eMeterType_Output*/ },
	{ nullptr }
};

//------------------------------------------------------------------------
AAX_Plugin_Desc effPlugins[] = {
	// effect-ID, name,	
	// Native ID, AudioSuite ID,
	// InChannels, OutChannels, InSideChain channels,
	// MIDI, Aux,
	// Meters
	// Latency
	// note: IDs must be unique across plugins

	// Mono variant
	{"@SMTG_PLUGIN_IDENTIFIER@.mono",
	 "@SMTG_PLUGIN_NAME@", 
	 CCONST (NATIVE_3ID, '1'),
	 CCONST (AUDIOSUITE_3ID, '1'),
	 1, /*mInputChannels*/
	 1, /*mOutputChannels*/
	 0, /*mSideChainInputChannels*/
	 effMIDI, /*effMIDI*/
	 nullptr, /*effAux*/
	 effMeters, /*effMeters*/
	 0 /*Latency*/ 
	},

	// Stereo variant
	{"@SMTG_PLUGIN_IDENTIFIER@.stereo",
	 "@SMTG_PLUGIN_NAME@", 
	 CCONST (NATIVE_3ID, '2'),
	 CCONST (AUDIOSUITE_3ID, '2'), 
	 2, /*mInputChannels*/
	 2, /*mOutputChannels*/
	 0, /*mSideChainInputChannels*/
	 effMIDI, /*effMIDI*/
	 nullptr, /*effAux*/
	 effMeters, /*effMeters*/
	 0 /*Latency*/
	},

	{nullptr}
};

//------------------------------------------------------------------------
AAX_Effect_Desc effDesc = {
	"@SMTG_VENDOR_NAME@",	// manufacturer
	"@SMTG_PLUGIN_NAME@",	// product
	SMTG_MANUFACTURER_ID, 	// manufacturer ID
	SMTG_PRODUCT_ID, 		// product ID
	@SMTG_PLUGIN_CLASS_NAME@VST3Category, // VST category (define XXXcids.h)
	{0},			// VST3 class ID (set later)
	1,				// version
	nullptr,		// no pagetable file "XXX.xml",
	effPlugins,
};

//------------------------------------------------------------------------
// this drag's in all the craft from the AAX library
int* forceLinkAAXWrapper = &AAXWrapper_linkAnchor;

//------------------------------------------------------------------------
AAX_Effect_Desc* AAXWrapper_GetDescription ()
{
	// cannot initialize in global descriptor, and it might be link order dependent
	memcpy (effDesc.mVST3PluginID, (const char*)k@SMTG_PLUGIN_CLASS_NAME@ProcessorUID,
	        sizeof (effDesc.mVST3PluginID));
	return &effDesc;
}
